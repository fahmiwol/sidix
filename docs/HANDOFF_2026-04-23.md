# Handoff — 2026-04-23 (Sprint 6.5 batch + kontinuitas agen)

Dokumen ini untuk **agen atau kontributor berikutnya**: ringkasan apa yang sudah jadi, cara verifikasi, dan urutan kerja yang disarankan.

---

## 1) Konteks git & workspace

| Item | Nilai |
|------|--------|
| **Branch aktif (lokal)** | `sociometer-sprint7` (tracking `origin/sociometer-sprint7`) |
| **Commit terakhir di HEAD** | `a9718ca` — `feat(brain_qa): wire Maqashid mode gate + Naskh di LearnAgent` |
| **Status working tree** | **Ada perubahan belum di-commit** (sprint 6.5: Raudah DAG, MinHash, CQF, intent, metrics, tes, benchmark, ekstensi scaffold, doc). |
| **Untracked (jangan commit sembarangan)** | Zip/folder scraping vendor-heavy, `uv.lock`, `brain/public/praxis/lessons/`, `185_agent_swarm_architecture.md`, dll. — **rapikan** via `.gitignore` atau commit terpisah setelah review. |

**Lanjut wajib infra:** `git add` (hanya path sprint) → `git commit` → `git push origin sociometer-sprint7` → buka/refresh PR ke `main` bila alur produksi mengikuti `main`.

---

## 2) Yang baru (Sprint 6.5 — kode & dokumen)

| Area | Path / artefak |
|------|----------------|
| **Raudah TaskGraph v0.2** | `brain/raudah/taskgraph.py`; `brain/raudah/core.py` (gelombang per peran, verifikator opsional); `brain/raudah/__init__.py` export |
| **MinHash dedup LearnAgent** | `apps/brain_qa/brain_qa/learn_agent.py` + `seen_minhash.json`; dependensi `datasketch` di `requirements.txt` |
| **CQF 10 kriteria** | `apps/brain_qa/brain_qa/cqf_rubrik.py` |
| **Intent classifier** | `apps/brain_qa/brain_qa/intent_classifier.py` |
| **Metrik runtime** | `apps/brain_qa/brain_qa/runtime_metrics.py`; bump di `agent_react.py` (Maqashid block/warn); `GET /agent/metrics` diperkaya di `agent_serve.py` (uptime, merge counter, `SIDIX_METRICS_SAMPLE_QUERY` → `intent_probe`) |
| **Tes** | `apps/brain_qa/tests/test_sprint6.py` (8 tes) |
| **Benchmark 50+20** | `apps/brain_qa/scripts/benchmark_sprint6.py` |
| **Chrome Social Radar** | Scaffold MV3: `browser/social-radar-extension/` (popup placeholder — belum integrasi API) |
| **Status & log** | `docs/STATUS_TODAY.md` (baris TODO 6.5); `docs/LIVING_LOG.md` (entri 2026-04-23) |

Maqashid + Naskh wiring sebelumnya tetap di `agent_react.py`, `learn_agent.py`, `agent_serve.py` (commit `a9718ca`).

---

## 3) Verifikasi lokal (Windows / PowerShell)

Dari `apps/brain_qa`:

```powershell
pip install -r requirements.txt
python -m pytest tests/ -q
python scripts/benchmark_sprint6.py
```

Harapan: **12 passed** (seluruh `tests/`), benchmark mencetak ringkasan Maqashid + intent untuk 70 query.

Catatan: `test_deduplicate_sha_identical` memakai UUID agar tidak bentrok dengan `seen_hashes` / `seen_minhash` persisten di mesin developer.

---

## 4) Produksi (baseline dari STATUS_TODAY)

- **Brain:** PM2, port **8765**, health OK per audit 2026-04-23.
- **Deploy setelah merge:** pull di VPS, install deps jika ada `datasketch`, restart PM2 untuk `brain_qa`.

---

## 5) Backlog “lanjut” (prioritas disarankan)

1. **Commit & push** hanya file sprint 6.5 (hindari zip/vendor noise).
2. **PR `sociometer-sprint7` → `main`** + smoke di staging/prod.
3. **Social Radar:** content script → cuplikan tab → `POST /agent/chat` (opsi redaksi PII); ikon status intent/sanad.
4. **Integrasi CQF** ke jalur evaluasi/lesson (opsional) — modul sudah siap dipanggil.
5. **Raudah:** uji e2e dengan Ollama hidup; tuning prompt agregasi.
6. **Rapikan untracked:** `.gitignore` untuk artefak scraping sementara; jangan memblokir kontributor.

---

## 6) Baca wajib singkat untuk sesi berikutnya

1. `docs/STATUS_TODAY.md` — TODO tabel + metrik produksi  
2. `docs/LIVING_LOG.md` — bagian **Log** paling baru  
3. `AGENTS.md` — aturan log + own-stack inference  

---

_Handoff ditulis 2026-04-23; selaraskan tanggal jika merge terjadi hari lain._
