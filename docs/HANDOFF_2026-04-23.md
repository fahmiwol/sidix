# HANDOFF 2026-04-23 — SIDIX v0.6.0 State Lengkap

**Dibuat oleh:** Agen Claude Code (sesi 2026-04-23)
**Baca urutan:** File ini → `docs/LIVING_LOG.md` (tail -50) → pilih task.
**Repo lokal:** `C:\SIDIX-AI\` (klon dari pohon utama GitHub — D: error, sudah dipulihkan)

---

## 1. Situasi Sekarang

SIDIX v0.6.0 per 2026-04-23. Rilis terakhir (347f803) di pohon utama GitHub sudah stabil.
D: drive telah dipulihkan — repo di-klon ulang ke `C:\SIDIX-AI\`.
Sesi ini mengadopsi rekomendasi dari analisis eksternal (April 2026) dan menghasilkan:
- **Maqashid Profiles v2** — filter tidak lagi memblokir creative output
- **Naskh Handler** — resolusi konflik knowledge berbasis sanad tier
- **Raudah Protocol v0.1** — skeleton orkestrasi multi-agen paralel
- **curator_agent.py patch** — score_gte_85 filter aktif
- **3 research notes baru** (183, 184, 185)

---

## 2. Topologi Produksi

| Komponen | Keterangan |
|----------|------------|
| Domain publik | app.sidixlab.com + ctrl.sidixlab.com (status: ONLINE) |
| Model aktif | sidix-lora:latest via Ollama (Qwen2.5-7B + LoRA) |
| Inferensi cadangan | qwen2.5:1.5b (ringan) |
| Image gen | SDXL RTX 3060 + tunnel (97s/gambar) |
| Cron aktif | 10 jadwal (LearnAgent 04:00, process_queue 04:30, optimizer 05:30 Senin) |
| Status standing-alone | ✅ kunci eksternal dinonaktifkan sejak 2026-04-21 |

---

## 3. Apa yang Baru di Sesi Ini

### File Baru

| File | Fungsi |
|------|--------|
| `apps/brain_qa/brain_qa/maqashid_profiles.py` | Filter Maqashid v2 mode-based |
| `apps/brain_qa/brain_qa/naskh_handler.py` | Resolusi konflik knowledge |
| `brain/raudah/__init__.py` | Raudah Protocol exports |
| `brain/raudah/core.py` | Orchestrator + Specialist + run_raudah() |
| `brain/public/research_notes/183_maqashid_profiles_mode_based.md` | Dokumentasi Maqashid v2 |
| `brain/public/research_notes/184_naskh_handler_knowledge_conflict.md` | Dokumentasi Naskh |
| `brain/public/research_notes/185_raudah_protocol_parallel_orchestration.md` | Dokumentasi Raudah |

### File Diubah

| File | Perubahan |
|------|-----------|
| `apps/brain_qa/brain_qa/curator_agent.py` | + PREMIUM_SCORE = 0.85, + _PREMIUM_FILE, filter premium aktif |
| `apps/brain_qa/brain_qa/agent_react.py` | + MAX_ACTION_REPEAT = 2, + MAX_TOOL_ERRORS = 3 |
| `docs/LIVING_LOG.md` | + entri sesi 2026-04-23 |
| `CHANGELOG.md` | + v0.6.0 entry |

---

## 4. Prioritas Sesi Berikutnya

### 🔴 SEGERA

**A. Wire Maqashid Profiles ke agent_react.py**
```python
# Di run_react(), sebelum return final_answer:
from .maqashid_profiles import evaluate_maqashid, get_mode_by_persona
mode = get_mode_by_persona(session.persona)
eval_result = evaluate_maqashid(session.question, session.final_answer, mode)
session.final_answer = eval_result["tagged_output"]
```

**B. test_sprint6.py — coverage Sprint 6**
- Test `log_accepted_output()` → `.data/accepted_outputs.jsonl` ditulis
- Test `generate_brand_kit(target_audience="UMKM Indonesia")` → no error
- Test `run_muhasabah_loop()` dengan mock CQF ≥ 7.0 → flywheel trigger
- Test `run_curation(dry_run=True)` → stats termasuk `premium_pairs` field

**C. Wire Naskh Handler ke learn_agent.py**
```python
# Di learn_agent.py saat add knowledge ke corpus:
from brain_qa.naskh_handler import NaskhHandler, note_to_knowledge_item
handler = NaskhHandler()
new_item = note_to_knowledge_item(content, path, topic)
corpus, logs = handler.add_to_corpus(new_item, existing_corpus)
```

### 🟡 SPRINT 6 LANJUTAN

**D. Raudah Protocol v0.2**
- TaskGraph DAG: dependency tracking (task B tidak mulai sebelum A selesai)
- Tambah endpoint `POST /raudah/run` ke agent_serve.py
- Tambah `GET /raudah/status/{session_id}` untuk progress tracking

**E. Agent endpoint `/swarm` → rename ke `/raudah`** di agent_serve.py (jika sudah ada)

**F. Copywriter.py** — AIDA/PAS/FAB formula, 3 varian per call (Sprint 4 backlog)

### 🟢 FOUNDATION (Sprint 4 backlog terbuka)

- [ ] `docs/SIDIX_BIBLE.md` upgrade → 7 Principles sebagai konstitusi
- [ ] ASPEC docstring template di semua modul `creative/`
- [ ] Skill definitions YAML (`definitions/content/ig_caption.yaml`, dll)

---

## 5. Research Note Berikutnya

Nomor: **186** (cek: `ls brain/public/research_notes/ | sort | tail -5`)

---

## 6. Mandat Aktif dari Pemilik Proyek

1. **Standing Alone** — SIDIX harus bisa berdiri sendiri, tanpa vendor API
2. **Creative Agent** — SIDIX jago design, advertising, branding, marketing, coding
3. **Maqashid tidak strict** — filter tidak boleh blokir creative output (v2 sudah fix)
4. **UI LOCK** — app.sidixlab.com chatboard BEKU, jangan ubah struktur
5. **Catat semua** — LIVING_LOG wajib tiap aksi signifikan
6. **Research note** — setiap task substantif wajib punya note di `brain/public/research_notes/`
7. **Jaga kerahasiaan** — pakai metafora untuk nama/identitas sensitif

---

## 7. Quick Commands

```powershell
# Dari C:\SIDIX-AI\
# Test curator dengan filter premium
$env:SIDIX_USE_MOCK_LLM=1
cd apps\brain_qa
python -m pytest tests\ -v

# Test Raudah Protocol (lokal)
python -c "import asyncio; from brain.raudah.core import run_raudah; print(asyncio.run(run_raudah('Test task sederhana')))"

# Cek jumlah research notes
ls brain\public\research_notes | Measure-Object

# Deploy ke produksi setelah push (via SSH ke server utama):
# git pull origin main && pm2 restart sidix-brain
```

---

*HANDOFF dibuat: 2026-04-23 | Versi: v0.6.0 | Sesi berikutnya: wire Maqashid v2 → ReAct + test coverage*
