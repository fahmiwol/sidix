# HANDOFF 2026-04-23 v2 — SIDIX v0.6.1 State Lengkap

**Dibuat oleh:** Agen Claude Code (sesi 2026-04-23 lanjutan)
**Baca urutan:** File ini → `docs/LIVING_LOG.md` (tail -60) → pilih task.
**Repo lokal:** `C:\SIDIX-AI\` (klon dari pohon utama GitHub)

---

## 1. Delta dari v0.6.0 (HANDOFF sebelumnya)

Sesi ini menambahkan:
- **Persona Rename** — 5 nama baru (AYMAN/ABOO/OOMAR/ALEY/UTZ) menggantikan nama lama
- **Backward Compat** — nama lama masih diterima via `_PERSONA_ALIAS` + alias di `_PERSONA_MODE_MAP`
- **Test update** — `test_persona.py` 20 test, 100% pass
- **Research Note 186** — dokumentasi rationale persona rename

---

## 2. Persona Baru (LOCK 2026-04-23)

| Nama Lama | Nama Baru | Karakter | Maqashid Mode |
|-----------|-----------|----------|---------------|
| MIGHAN    | **AYMAN** | Strategic Sage | IJTIHAD |
| TOARD     | **ABOO**  | The Analyst    | ACADEMIC |
| FACH      | **OOMAR** | The Craftsman  | IJTIHAD |
| HAYFAR    | **ALEY**  | The Learner    | GENERAL |
| INAN      | **UTZ**   | The Generalist | CREATIVE |

**Default persona:** AYMAN (API/serve), UTZ (agent_react)

---

## 3. Topologi Produksi (sama dari v0.6.0)

| Komponen | Keterangan |
|----------|------------|
| Domain publik | app.sidixlab.com + ctrl.sidixlab.com (status: ONLINE) |
| Model aktif | sidix-lora:latest via Ollama (Qwen2.5-7B + LoRA) |
| Inferensi cadangan | qwen2.5:1.5b (ringan) |
| Image gen | SDXL RTX 3060 + tunnel (97s/gambar) |
| Cron aktif | 10 jadwal (LearnAgent 04:00, process_queue 04:30, optimizer 05:30 Senin) |
| Status standing-alone | ✅ kunci eksternal dinonaktifkan sejak 2026-04-21 |

---

## 4. File yang Diubah di Sesi Ini

| File | Perubahan |
|------|-----------|
| `apps/brain_qa/brain_qa/persona.py` | Nama baru + backward compat alias + semua scoring |
| `apps/brain_qa/brain_qa/maqashid_profiles.py` | `_PERSONA_MODE_MAP` update + default UTZ |
| `apps/brain_qa/brain_qa/serve.py` | Literal type + default AYMAN |
| `apps/brain_qa/brain_qa/agent_react.py` | Default UTZ |
| `SIDIX_USER_UI/src/api.ts` | Type Persona + default AYMAN |
| `SIDIX_USER_UI/src/main.ts` | Default AYMAN |
| `SIDIX_USER_UI/index.html` | Persona selector options |
| `tests/test_persona.py` | 20 test, backward compat verified |
| `brain/public/research_notes/186_persona_rename_*.md` | Dokumentasi rename |
| `docs/LIVING_LOG.md` | +entri sesi 2026-04-23 v2 |

---

## 5. Prioritas Sesi Berikutnya

### 🔴 SEGERA

**A. Wire Maqashid Profiles ke agent_react.py** ← PALING PENTING
```python
# Di run_react(), sebelum return final_answer:
from .maqashid_profiles import evaluate_maqashid, get_mode_by_persona
mode = get_mode_by_persona(session.persona)
eval_result = evaluate_maqashid(session.question, session.final_answer, mode)
session.final_answer = eval_result["tagged_output"]
```

**B. Build + Deploy frontend** (persona selector sudah nama baru, perlu rebuild)
```powershell
# Di nodus utama setelah push:
# cd /opt/sidix/SIDIX_USER_UI && npm run build && pm2 restart sidix-ui
```

**C. test_sprint6.py — coverage Sprint 6**
- Test `log_accepted_output()` → `.data/accepted_outputs.jsonl` ditulis
- Test `generate_brand_kit(target_audience="UMKM Indonesia")` → no error
- Test `run_muhasabah_loop()` dengan mock CQF ≥ 7.0 → flywheel trigger
- Test `run_curation(dry_run=True)` → stats termasuk `premium_pairs` field

**D. Wire Naskh Handler ke learn_agent.py**
```python
from brain_qa.naskh_handler import NaskhHandler, note_to_knowledge_item
handler = NaskhHandler()
new_item = note_to_knowledge_item(content, path, topic)
corpus, logs = handler.add_to_corpus(new_item, existing_corpus)
```

### 🟡 SPRINT 6 LANJUTAN

**E. Raudah Protocol v0.2**
- TaskGraph DAG: dependency tracking (task B tidak mulai sebelum A selesai)
- Tambah endpoint `POST /raudah/run` ke agent_serve.py
- Tambah `GET /raudah/status/{session_id}` untuk progress tracking

**F. /metrics endpoint ringan** (rekomendasi Kimi section 10)
```python
@app.get("/metrics")
def metrics():
    return {
        "tool_calls_total": counter,
        "errors_total": error_counter,
        "avg_cqf_score": cqf_avg,
        "uptime_s": int(time.time() - START_TIME)
    }
```

**G. Progress indicator UI** untuk saat Raudah Protocol berjalan (multi-step)

### 🟢 FOUNDATION

- [ ] `docs/SIDIX_BIBLE.md` upgrade → 7 Principles sebagai konstitusi
- [ ] Copywriter.py — AIDA/PAS/FAB formula, 3 varian per call
- [ ] ASPEC docstring template di semua modul `creative/`

---

## 6. Research Note Berikutnya

Nomor: **187** (cek: `ls brain/public/research_notes/ | sort | tail -5`)

---

## 7. Kimi Insights Status (dari f12a7c7c HTML)

| Item | Status |
|------|--------|
| Maqashid v2 (mode-based) | ✅ Done (v0.6.0) |
| Naskh Handler | ✅ Done (v0.6.0) |
| Raudah Protocol v0.1 | ✅ Done (v0.6.0) |
| MAX_ACTION_REPEAT + MAX_TOOL_ERRORS | ✅ Done (v0.6.0) |
| Wire Maqashid ke run_react() | ⏳ Pending |
| Wire Naskh ke corpus pipeline | ⏳ Pending |
| /metrics endpoint | ⏳ Pending |
| Progress bar Raudah di UI | ⏳ Pending |
| Benchmark test 20 creative + 10 harmful | ⏳ Pending |

---

## 8. Mandat Aktif dari Pemilik Proyek

1. **Standing Alone** — SIDIX harus bisa berdiri sendiri, tanpa vendor API
2. **Creative Agent** — SIDIX jago design, advertising, branding, marketing, coding
3. **Maqashid tidak strict** — filter tidak boleh blokir creative output (v2 sudah fix)
4. **UI LOCK** — app.sidixlab.com chatboard BEKU, jangan ubah struktur
5. **Catat semua** — LIVING_LOG wajib tiap aksi signifikan
6. **Research note** — setiap task substantif wajib punya note di `brain/public/research_notes/`
7. **Jaga kerahasiaan** — pakai metafora untuk nama/identitas sensitif

---

## 9. Quick Commands

```powershell
# Dari C:\SIDIX-AI\
# Test persona routing
$env:SIDIX_USE_MOCK_LLM=1
python -m pytest tests\test_persona.py -v

# Test maqashid profiles
python -c "from apps.brain_qa.brain_qa.maqashid_profiles import evaluate_maqashid, MaqashidMode; print(evaluate_maqashid('buatkan iklan kopi', 'Kopi kami terbaik!', MaqashidMode.CREATIVE))"

# Cek jumlah research notes
ls brain\public\research_notes | Measure-Object

# Build frontend setelah persona update
# cd SIDIX_USER_UI && npm run build
```

---

*HANDOFF dibuat: 2026-04-23 v2 | Versi: v0.6.1 | Sesi berikutnya: wire Maqashid v2 → ReAct + frontend build*
