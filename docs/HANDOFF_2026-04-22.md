# HANDOFF 2026-04-22 — SIDIX State Lengkap
**Dibuat oleh:** Claude Code (sesi 2026-04-22)
**Tujuan:** Sesi baru bisa langsung lanjut tanpa audit ulang dari nol.
**Baca urutan:** File ini → `docs/LIVING_LOG.md` (tail -30) → pilih task.

---

## 1. Situasi Sekarang (1 paragraf)

SIDIX per 2026-04-22 adalah AI agent standing-alone berbasis Qwen2.5-7B + LoRA adapter custom, live di `sidixlab.com`. Backend `ctrl.sidixlab.com` ONLINE: `model_ready=true`, `tools_available=35`, 10 cron aktif, 181+ research notes, 89 modul Python. Sprint 5 (curator/debate/agency_kit/llm_judge/prompt_optimizer) dan Sprint 6 Quick Wins (flywheel L1 + signature fix + cron endpoint) **selesai dan live**. Platform identity final: GitHub=`tiranyx/sidix`, HuggingFace=`Tiranyx/sidix-lora` (live 78MB adapter), domain=`sidixlab.com`. Merge conflict di LIVING_LOG sudah diselesaikan sesi ini. Sesi berikutnya: `curator_agent` score_gte_85 → test coverage → Sprint 6 full (3D/Voyager).

---

## 2. VPS State

| Item | Nilai |
|------|-------|
| Branch main | `d95e276` (post Sprint 6 quick wins) |
| PM2 processes | `sidix-brain` (pid ~144xxx) + `sidix-ui` + galantara + tiranyx + abra + shopee-gateway |
| `tools_available` | **35** |
| `model_ready` | `true` |
| Model | `sidix-lora:latest` via Ollama (Qwen2.5-7B + LoRA) + `qwen2.5:1.5b` |
| Swap | 4GB ditambah (RAM 5GB free + Swap 4GB free) |
| `qwen2.5:7b` | Dihapus (duplikat — hemat 4.7GB) |
| Standing Alone | GROQ + GEMINI keys dinonaktifkan di `.env` (`# DISABLED_STANDALONE:`) |
| Fallback chain | Local → Mock (jujur, bukan diam-diam pakai vendor) |
| Image gen | SDXL laptop RTX 3060 + ngrok `scribble-trimness-alike.ngrok-free.dev` |
| Image speed | ~97s E2E via VPS+tunnel (512x512 15 steps) |

---

## 3. Cron Aktif

| Jadwal | Command | Tujuan |
|--------|---------|--------|
| `0 4 * * *` | LearnAgent | Fetch arXiv/Wikipedia/MusicBrainz/GitHub/Quran |
| `30 4 * * *` | process_queue | Parse draft → corpus queue |
| `30 5 * * MON` | prompt_optimizer | Weekly L1 self-evolution (dipasang via SSH) |
| 9 cron Threads | various | Auto-post series Threads harian |
| Startup | startup-fetch.bat | Fetch Wikipedia per login |

---

## 4. Yang BELUM Selesai — Prioritas

### 🔴 SEGERA (quick wins, S effort)

#### A. `curator_agent.py` — score_gte_85 filter
- **File:** `apps/brain_qa/brain_qa/curator_agent.py`
- **Task:** Di `run_curation()`, pisahkan pasangan dengan `score > 8.5` ke `.data/lora_premium_pairs.jsonl`
- **Kenapa:** Feed LoRA tier premium — data terbaik → training terfokus
- **Ref:** `docs/SPRINT5_ADOPTION_PLAN.md §3.2`

```python
# Tambahan di run_curation() setelah filtering utama:
premium_pairs = [p for p in approved_pairs if p.get("score", 0) > 8.5]
if premium_pairs:
    _append_jsonl(PREMIUM_PAIRS_FILE, premium_pairs)
```

#### B. Test Coverage Sprint 6
- **File:** Buat `apps/brain_qa/tests/test_sprint6.py`
- Test `log_accepted_output()` → verify `.data/accepted_outputs.jsonl` ditulis
- Test `generate_brand_kit(target_audience="UMKM Indonesia")` → no error
- Test `run_muhasabah_loop()` dengan mock output CQF ≥ 7.0 → flywheel trigger

### 🟡 SPRINT 6 FULL (pilih satu per sesi)

#### C. 3D Pipeline (user directive "all about 3D")
- Buat `apps/brain_qa/brain_qa/creative/gen_3d.py`
- Stubs: `text_to_3d()`, `image_to_3d()`, `optimize_mesh()`
- Wire ke `agent_tools.py` sebagai tool `gen_3d` + `image_to_3d`
- Ref: `docs/MASTER_ROADMAP_2026-2027.md §Sprint 6`

#### D. Voyager Protocol (HIGH RISK — review dulu)
- SIDIX auto-generate tool Python baru saat butuh kapabilitas baru
- AST security scan wajib sebelum exec
- File: `apps/brain_qa/brain_qa/voyager_protocol.py`
- Ref: note 169, `docs/CREATIVE_AGENT_TAXONOMY.md`

### 🟢 SPRINT 4 SISA (kanban terbuka)

**Foundation Adoption:**
- [ ] Upgrade `docs/SIDIX_BIBLE.md` → 7 Principles sebagai konstitusi
- [ ] `muhasabah_loop.py` integrasi dengan `vision_tracker.py`
- [ ] Skill definitions YAML (`definitions/content/ig_caption.yaml`, dll)
- [ ] ASPEC docstring template di semua modul `apps/brain_qa/brain_qa/creative/`

**P0 Creative Agents (sprint 4 yang belum):**
- [ ] `copywriter.py` — AIDA/PAS/FAB formula, 3 varian per call
- [ ] `thumbnail_generator.py` — text_to_image integration + overlay teks
- [ ] `campaign_strategist.py` — AARRR funnel + channel mix + timeline
- [ ] `ads_generator.py` — FB/Google/TikTok ad copy + image prompts

**Launch Preparation:**
- [ ] Landing `sidixlab.com` update (hero "AI Agent Indonesia Gratis", demo section)
- [ ] Threads launch series 5 post + 8 sample image
- [ ] GitHub README demo screenshot + Release v0.1.0-beta tag

---

## 5. Manual TODO (butuh Fahmi langsung)

| # | Task | Cara |
|---|------|------|
| 1 | Rotate Google OAuth Client Secret (exposed di chat) | Google Cloud Console → Credentials → Regenerate |
| 2 | Cek power plan laptop saat ngrok live | Windows → Power Plan → Never Sleep |
| 3 | Monitor ngrok URL setelah restart (URL bisa berubah) | Update `SIDIX_IMAGE_GEN_URL` di VPS `.env` kalau berubah |
| 4 | Apply Google Adsense tiranyx.co.id (terpisah dari SIDIX) | Butuh 5+ artikel dulu |

---

## 6. File Kunci

```
D:\MIGHAN Model\
├── CLAUDE.md                            ← SSOT aturan + UI LOCK
├── AGENTS.md                            ← preferensi + workspace facts
├── apps/brain_qa/brain_qa/
│   ├── curator_agent.py                 ← NEXT: score_gte_85
│   ├── muhasabah_loop.py                ← flywheel patch (T6.1)
│   ├── brand_builder.py                 ← target_audience (T6.2)
│   ├── content_planner.py               ← target_audience (T6.2)
│   ├── creative_quality.py              ← CQF scorer (Sprint 4)
│   ├── debate_ring.py                   ← multi-agent (Sprint 5)
│   ├── agency_kit.py                    ← 1-click kit (Sprint 5)
│   ├── llm_judge.py                     ← CQF judge (Sprint 5)
│   ├── prompt_optimizer.py              ← L1 self-evolution (Sprint 5)
│   ├── agent_tools.py                   ← 35 tools registry
│   └── agent_serve.py                   ← FastAPI + semua endpoint
├── apps/brain_qa/tests/
│   ├── test_sprint5.py                  ← referensi pola test
│   └── test_sprint6.py                  ← BUAT (belum ada)
├── docs/
│   ├── LIVING_LOG.md                    ← append SETIAP aksi
│   ├── MASTER_ROADMAP_2026-2027.md      ← SSoT sprint canonical
│   ├── SPRINT5_ADOPTION_PLAN.md         ← gap analysis quick wins
│   ├── CREATIVE_AGENT_TAXONOMY.md       ← 10 domain × 37 agent tracker
│   └── decisions/ADR_002_*.md           ← killer offer strategy
└── brain/public/research_notes/
    └── 182_*.md                         ← terakhir, berikutnya: 183+
```

---

## 7. Research Note Berikutnya

Cek: `ls "D:\MIGHAN Model\brain\public\research_notes\" | sort | tail -5`
Lanjutkan dari nomor **183**.

---

## 8. Mandat User yang Masih Berlaku

1. **Standing Alone** — SIDIX harus bisa berdiri sendiri, bukan bergantung vendor API
2. **All about 3D** — Sprint 6+ harus ada 3D pipeline
3. **SIDIX bukan search engine** — SIDIX harus ACTION (generate/execute), bukan tampilkan dari corpus
4. **UI LOCK** — app.sidixlab.com chatboard FROZEN, jangan ubah struktur
5. **Catat semua** — LIVING_LOG wajib tiap aksi signifikan
6. **Research note** — setiap task substantif wajib punya note di `brain/public/research_notes/`
7. **SIDIX jago coding** — coding corpus + tools sudah ada, perlu diperluas

---

## 9. Quick Commands

```powershell
# Start local dev (dari D:\MIGHAN Model\)
.\scripts\tasks.ps1 serve      # backend port 8765
.\scripts\tasks.ps1 ui         # frontend port 3000

# Run tests
$env:SIDIX_USE_MOCK_LLM=1; cd apps\brain_qa; python -m pytest tests\ -v

# Reindex corpus
cd apps\brain_qa; python -m brain_qa index

# Cek health VPS
curl https://ctrl.sidixlab.com/health

# Deploy ke VPS (setelah commit + push)
# SSH ke VPS: git pull origin main && pm2 restart sidix-brain
```

---

*HANDOFF dibuat: 2026-04-22 | Sesi berikutnya: curator_agent → test → Sprint 6*
