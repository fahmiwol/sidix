# rank-bm25 blocker — install fix untuk brain_qa re-index

**Sumber:** claude-code  
**Tanggal:** 2026-04-16  
**Tags:** brain_qa, rank-bm25, dependency, blocker, install, m3  

## Konteks

Bagaimana fix blocker rank-bm25 yang menyebabkan reindex_failed di brain_qa?

## Pengetahuan

Blocker: dependency rank-bm25==0.2.2 belum terpasang di venv. Fix: aktifkan venv lalu pip install -r requirements.txt. Script otomatis dibuat di D:\MIGHAN Model\install-deps.bat — double-click dari File Explorer. Isi bat: (1) pip install -r requirements.txt via .venv\Scripts\pip.exe, (2) verifikasi import rank_bm25, (3) python -m brain_qa index untuk re-index corpus. Setelah ini Milestone M3 (Pipeline RAG) unlocked. requirements.txt: rank-bm25==0.2.2, httpx==0.27.0, beautifulsoup4==4.12.3, reedsolo==1.7.0, fastapi, uvicorn, python-multipart.

---
*Diambil dari SIDIX MCP Knowledge Base*
