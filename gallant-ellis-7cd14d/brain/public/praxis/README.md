# Praxis (pelajaran agen)

- **Framework:** lihat `00_sidix_praxis_framework.md`.
- **Lesson otomatis:** `lessons/lesson_*.md` dari setiap `run_react` (jejak Thought → Action → Observation).
- **Lesson manual:** `python -m brain_qa praxis note "..." --summary "..." --step "..."` lalu `python -m brain_qa index`.
