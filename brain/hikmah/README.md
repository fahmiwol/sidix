# Hikmah — Self-Train (Pilar 7)

**Delegasi:** pipeline fine-tuning / adapter (mis. LoRA) dan artefak di `apps/brain_qa/models/`.

Folder ini menandai **pilar pembelajaran bobot**; tidak menyimpan bobot model di git.
