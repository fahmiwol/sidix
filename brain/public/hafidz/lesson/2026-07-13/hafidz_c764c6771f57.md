---
title: "Bagaimana cara reverse string di Python?"
date: 2026-07-13
sanad_tier: lesson
sanad_score: 0.600
persona: UTZ
sources: ['corpus', 'web_search', 'dense_index']
tools: ['corpus_search', 'dense_search', 'web_search']
knowledge_id: hafidz_c764c6771f57
auto_generated: true
store_type: lesson
failure_context: |
  Consensus score 0.60 below threshold 0.92. General quality issue.
---

# Bagaimana cara reverse string di Python?

## Jawaban (FAILED)

Cara membalikkan String di Python:

1. Menggunakan perulangan for: 
   ```python
   def reverse_string(string):
       # an empty string for storing reversed string
       reversed_string = ""
       
       # looping through the string
       for char in string:
           # reversing the character
           reversed_string = char + reversed_string
   
       return reversed_string
   ```

2. Menggunakan notasi irisan [::-1]: 
   ```python
   def reverse_string(string):
       # an empty string for storing reversed string
       reversed_string = ""
       
       # using slice to reverse the string
       reversed_string = string[::-1]
   
       return reversed_string
   ```

3. Menggunakan fungsi reversed(): 
   ```python
   from itertools import izip

   def reverse_string(string):
       # an empty string for storing reversed string
       reversed_string = ""
       
       # using reversed() function to reverse the string
       reversed_string = ''.join(reversed(string))
   
       return reversed_string
   ```

4. Menggunakan fungsi join(): 
   ```python
   def reverse_string(string):
       # an empty string for storing reversed string
       reversed_string = ""
       
       # using join() function to reverse the string
       reversed_string = ''.join(reversed(string))
   
       return reversed_string
   ```

5. Menggunakan fungsi slice: 
   ```python
   def reverse_string(string):
       # an empty string for storing reversed string
       reversed_string = ""
       
       # using slice to reverse the string
       reversed_string = string[::-1]
   
       return reversed_string
   ```

6. Menggunakan slicing dengan step -1: 
   ```python
   def reverse_string(string):
       # an empty string for storing reversed string
       reversed_string = ""
       
       # using slice with step -1 to reverse the string
       reversed_string = string[::-1]
   
       return reversed_string
   ```

7. Menggunakan perulangan while: 
   ```python
   def reverse_string(string):
       # an empty string for storing reversed string
       reversed_string = ""
       
       # looping through the string with a step of -1
       for i in range(len(string)-1, -1, -1):
           # reversing the character
           char = string[i]
           reversed_string += char
   
       return reversed_string
   ```

8. Menggunakan fungsi list reverse(): 
   ```python
   def reverse_string(string):
       # an empty string for storing reversed string
       reversed_string = ""
       
       # using list reverse() function to reverse the string
       reversed_string = ''.join(reversed(list(string)))
   
       return reversed_string
   ```

9. Menggunakan perulangan while dengan step -1: 
   ```python
   def reverse_string(string):
       # an empty string

## Failure Context

Consensus score 0.60 below threshold 0.92. General quality issue.

## Metadata

- **sanad_score**: 0.600
- **failure_context**: Consensus score 0.60 below threshold 0.92. General quality issue.
- **sources_used**: corpus, web_search, dense_index
- **tools_used**: corpus_search, dense_search, web_search
- **stored_at**: 2026-07-13T06:04:31.106060+00:00
- **persona**: UTZ
